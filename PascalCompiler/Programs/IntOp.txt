program IntOp;
var
  a,x,y,z : integer;
  multiply: integer;
begin
  x := 100;
  y := 50;
  z := 10;
  writeln("X: ",x);
  writeln("Y: ",y);
  writeln("Z: ",z);
  a := 5;
  writeln("A: ", a);
  multiply := a * z;
  writeln("Mult: ",multiply);
end.
